[
  {
    "id": "vault-01",
    "question": "What is the rule when a Director's own PR is ready to merge -- can it merge that PR itself?",
    "expected_source": "vault-fact",
    "expected_identifier": "20260902040000.md",
    "note": "agent-estate#1333: the vault fact-file naming convention migrated from descriptive slugs (author-never-self-merges.md) to timestamp-id filenames (memory-conventions' own 01 - Notes/<id>.md layout) sometime after this case was authored; the OLD file no longer exists under that name at all, so the identifier check could never pass regardless of ranking. Re-pinned to the current file via its own frontmatter aliases: [\"author-never-self-merges\"] (verified 2026-09-10, grep -rl 'aliases:.*\"author-never-self-merges\"'). Still MISSES top-3 even with the corrected identifier (not in top 10 as of 2026-09-10) -- several other vault facts independently restate 'do not merge your own PRs' (content duplication, not a ranking bug), and BM25 splits the signal across them. Covered by retrievalMaxMisses, not by this rename.",
    "rationale": "The vault fact is titled 'Author never self-merges -- retained' and states directly: 'Merge authority is not permission to merge your own work.' It is the authoritative statement of this rule, not a paraphrase of it."
  },
  {
    "id": "vault-02",
    "question": "Why do all the agents in a tmux session suddenly look logged out at the same time?",
    "expected_source": "vault-fact",
    "expected_identifier": "20260824040000.md",
    "note": "authored on the premise that Query only indexes tier1 (title + description) and never the fact's own body -- that premise is stale (agent-estate#1339): the vault reader includes the fact's full body in Tier2 (agent-estate#1027), which query.go's searchableText already reads and BM25-scores, and 'tmux' itself is also already in this fact's own frontmatter tags (SynapticTags, folded into the tier1-weighted 3x text, not merely the unweighted body) -- so 'tmux' was never actually unindexed. 'Logged out' still does not appear verbatim anywhere in the fact (its own text says loggedIn: false, a different phrasing), so a real term-overlap gap survives for that half of the question; this case still exercises reworded phrasing, just not for the reason originally written. agent-estate#1333: identifier re-pinned from the retired slug filename (a-locked-keychain-looks-like-a-tmux-session-problem.md, which no longer exists) to the current timestamp-id file via its own frontmatter aliases field (verified 2026-09-10). Still MISSES (not in top 10) even corrected -- agent-estate#1339 measured this is ordinary vault/corpus-growth term-volume dilution (removing every near-duplicate Parameter item left this fact's own rank unchanged), not unindexed content; covered by retrievalMaxMisses.",
    "rationale": "This exact symptom -- 'every tmux-hosted agent reports {\"loggedIn\": false}' -- is the fact's own documented incident (agent-supervisor#582). It is the one file that explains the cause (a locked login keychain), so it is authoritative regardless of whether the current ranking finds it."
  },
  {
    "id": "vault-03",
    "question": "What happened during the week Jon had already ruled out the shell and Python supervisor but it kept getting maintained anyway?",
    "expected_source": "vault-fact",
    "expected_identifier": "20260830040000.md",
    "note": "agent-estate#1333: identifier re-pinned from the retired slug filename (check-the-directive-before-the-work.md, which no longer exists) to the current timestamp-id file via its own frontmatter aliases field (verified 2026-09-10, grep -rl 'aliases:.*\"check-the-directive-before-the-work\"' -- exactly one match). Rank 1 with the corrected identifier.",
    "rationale": "The fact documents this exact week: '493 issues, 159 open... Jon had already ruled that layer out. He ended the week with nothing delivered and said so.' No other fact covers this incident."
  },
  {
    "id": "vault-04",
    "question": "What is the recurring defect where someone fixes the body of a document but leaves the title or heading asserting the old thing?",
    "expected_source": "vault-fact",
    "expected_identifier": "20260811165100.md",
    "note": "agent-estate#1333: identifier re-pinned from the retired slug filename (correct-the-label-not-just-the-body.md, which no longer exists) to the current timestamp-id file via its own frontmatter aliases field (verified 2026-09-10, grep -rl 'aliases:.*\"correct-the-label-not-just-the-body\"' -- exactly one match). Rank 1 with the corrected identifier.",
    "rationale": "The fact's own title states this pattern verbatim: 'Fixing the body and leaving the label is the most repeated defect -- the label is what gets trusted.'"
  },
  {
    "id": "corpus-01",
    "question": "Does the tmux theme need to match Jon's own dotfiles exactly?",
    "expected_source": "vault-fact",
    "expected_identifier": "20260726174037.md",
    "note": "agent-estate#1333: live_parameters row it-ac0a4ce618bc5fd7 (this case's original target) is still present in the corpus DB unchanged, but no longer stands alone in the compiled index under corpus:item:it-ac0a4ce618bc5fd7 -- the knowledge-workflow's publish step (AGENTS.md's register/inspect/propose/review/publish/retrieve/refresh pipeline) has since projected this same parameter into the vault as its own note, title 'tmux_theme=match_jons_dotfiles' (verified 2026-09-10 via direct sqlite3 read of live_parameters plus grep -rl on the vault for that exact resolved_to string -- exactly one match). Re-pinned to that note, which now ranks 1. This is the knowledge system doing its documented job (corpus parameter -> curated vault note), not a retrieval defect.",
    "rationale": "live_parameters row it-ac0a4ce618bc5fd7 resolves to 'tmux_theme=match_jons_dotfiles' with body 'The tmux theme must match Jon's own dotfiles exactly, not an approximation.' -- a direct, hard parameter answer to this exact question."
  },
  {
    "id": "corpus-02",
    "question": "What should happen when a PR is refused for being behind main?",
    "expected_source": "vault-fact",
    "expected_identifier": "20260805204630.md",
    "note": "agent-estate#1333: same knowledge-workflow publish pattern as corpus-01's own note -- live_parameters row it-d0db3ce9917a1a5c is unchanged in the corpus DB but the compiled index no longer carries it standalone; it has been projected into the vault as its own note, title 'behind_pr=update_branch_never_admin_merge' (verified 2026-09-10, exactly one vault match on that resolved_to string). Re-pinned to that note, which now ranks 2.",
    "rationale": "live_parameters row it-d0db3ce9917a1a5c resolves to 'behind_pr=update_branch_never_admin_merge': 'When a PR is refused as BEHIND, update the branch and let CI re-run rather than reaching for --admin.'"
  },
  {
    "id": "corpus-03",
    "question": "Am I allowed to merge my own green, mergeable PRs instead of leaving them for Jon?",
    "expected_source": "corpus-parameter",
    "expected_identifier": "corpus:item:it-c403028cab9d7bc8",
    "note": "agent-estate#1333: deliberately LEFT UNCHANGED, unlike corpus-01/02/04's own identical-shaped fix. This row too has been projected into the vault (title 'green_pr=merge_without_jon', 01p - Parameters/20260726064621.md, verified 2026-09-10) but re-pinning it there would leave zero cases.json cases with expected_source=corpus-parameter, failing TestCoversAllFiveSources (goldenset_test.go), which requires every one of the five sources -- corpus-parameter included -- to have a live case. Kept pointing at the original corpus:item identifier specifically so that requirement stays real rather than vacuously true. Still MISSES (rank 5 even against the vault-projected note, per direct query 2026-09-10) -- covered by retrievalMaxMisses like vault-01/vault-02.",
    "rationale": "live_parameters row it-c403028cab9d7bc8 resolves to 'green_pr=merge_without_jon': 'Do not park green, mergeable PRs on me -- merge them yourself and only escalate the genuinely gated ones.'"
  },
  {
    "id": "corpus-04",
    "question": "What does CLEAN actually mean when git reports a PR has no merge conflict?",
    "expected_source": "vault-fact",
    "expected_identifier": "20260803235230.md",
    "note": "agent-estate#1333: same knowledge-workflow publish pattern as corpus-01/corpus-02's own notes -- live_parameters row it-2c9e97e40325747a is unchanged in the corpus DB but the compiled index no longer carries it standalone; it has been projected into the vault as its own note, title 'merge_status=clean_is_textual_not_semantic' (verified 2026-09-10, exactly one vault match on that resolved_to string). Re-pinned to that note, which now ranks 2.",
    "rationale": "live_parameters row it-2c9e97e40325747a resolves to 'merge_status=clean_is_textual_not_semantic': 'CLEAN means git found no textual conflict, not that the result is coherent.'"
  },
  {
    "id": "stars-01",
    "question": "Which starred repo lets an AI agent capture macOS screenshots through an MCP server?",
    "expected_source": "github-stars",
    "expected_identifier": "https://github.com/openclaw/Peekaboo",
    "rationale": "openclaw/Peekaboo's own starred description: 'a macOS CLI & optional MCP server that enables AI agents to capture screenshots of applications, or the entire system, with optional visual question answering.' No other starred repo matches this description."
  },
  {
    "id": "stars-02",
    "question": "Is there a starred tool that puts a colored frame and name tag on every Mac in a fleet so I stop confusing them over remote desktop?",
    "expected_source": "github-stars",
    "expected_identifier": "https://github.com/steipete/Nameplate",
    "rationale": "steipete/Nameplate's own starred description: 'Brand every Mac in your fleet -- colored frame, name tag, watermark, and connect splash as click-through overlays. Never wonder which Mac you remoted into again.'"
  },
  {
    "id": "stars-03",
    "question": "Which starred project turns a codebase, its docs, SQL schemas and configs into a queryable knowledge graph without a vector store?",
    "expected_source": "github-stars",
    "expected_identifier": "https://github.com/Graphify-Labs/graphify",
    "rationale": "Graphify-Labs/graphify's own starred description: 'Turn any codebase, with its docs, SQL schemas, configs, and PDFs, into a queryable knowledge graph... local deterministic AST parsing, every edge explained, no vector store.'"
  },
  {
    "id": "loops-01",
    "question": "Which loops-research chapter says generation is now cheap and a loop's speed is bottlenecked by its verification half?",
    "expected_source": "loops-research",
    "expected_identifier": "04-verifiers.md",
    "rationale": "04-verifiers.md opens with exactly this claim: 'generation is cheap now; the loop spins only as fast as its verification half.' It is the file's own first paragraph, not a summary composed elsewhere."
  },
  {
    "id": "loops-02",
    "question": "Which loops-research note says persistent memory remains underdeveloped in real agent loops?",
    "expected_source": "loops-research",
    "expected_identifier": "09-memory.md",
    "rationale": "09-memory.md's own first paragraph: 'the survey's finding that persistent memory remains underdeveloped in real loops makes this one of the two biggest greenfield opportunities.'"
  },
  {
    "id": "loops-03",
    "question": "What does the loops-research corpus say about triggers being the contract field most loops still get wrong, since most are kicked off by hand?",
    "expected_source": "loops-research",
    "expected_identifier": "03-triggers.md",
    "rationale": "03-triggers.md's own first paragraph: 'A trigger is the first contract field and the one the surveyed corpus is weakest on... Most loops in the wild are still manually kicked.'"
  },
  {
    "id": "docs-01",
    "question": "What does this repo's own AGENTS.md say about capturing exit codes instead of piping a command through something like tail?",
    "expected_source": "repo-docs",
    "expected_identifier": "docs/orientation/invariants.md#the-daemon-invariants-and-failure-modes--the-failure-mode-this-codebase-produces-most",
    "note": "agent-estate#1333: identifier re-pinned. AGENTS.md's own current text records the move: 'The detailed orientation that used to live here was relocated verbatim to docs/orientation/.' The failure-mode section this case targets moved with it -- same heading text, same content, new file -- so the old AGENTS.md#... suffix can no longer match anything. Verified 2026-09-10 that docs/orientation/invariants.md carries this exact section (same anchor tail) and ranks 2. The question wording ('this repo's own AGENTS.md say') is now slightly dated by the same move but is left as-is since AGENTS.md is still accurately described in this repo as the entry point that routes here.",
    "rationale": "AGENTS.md's own \"The failure mode this codebase produces most\" section states this rule verbatim: 'Capture exit codes directly. `cmd | tail` gives you `tail`'s status.' Before this source existed, AGENTS.md/CLAUDE.md was not indexed at all -- this is the exact rule the #1034 issue names as the motivating case."
  },
  {
    "id": "docs-02",
    "question": "What did the retired collision-check.sh guard refuse, and how was a lane considered 'in flight'?",
    "expected_source": "repo-docs",
    "expected_identifier": "docs/historical/ci-rules-retired.md#ci-rules-retired-with-the-shell-supervisor--rules-retired-with-their-scripts-not-with-a-workflow",
    "rationale": "docs/historical/ci-rules-retired.md's \"Rules retired with their scripts, not with a workflow\" section states it directly: collision-check.sh refused 'a dispatch whose files overlap an in-flight lane's files', and 'In flight' is defined there as 'every ledger task not recorded complete/failed/cancelled that has a recorded worktree path.' No other file in the index carries this."
  },
  {
    "id": "camelcase-01",
    "question": "If a dispatch to an agent times out, should the timeout path record the failure the same way an ordinary failed dispatch already does, instead of leaving it looking like a silent success?",
    "expected_source": "corpus-directive",
    "expected_identifier": "corpus:item:it-1b08d9b19b25eee7",
    "note": "agent-estate#1150's corpus-directive stratum: this item's own body names its dispatch-delivery routing check with a single run-together identifier joining three ordinary English words with no internal separator or space -- the exact shape splitWords does not currently break apart. This rationale and question deliberately do not reproduce that identifier or any other of the item's own wording (no corpus body text may live in this repo, fixtures included) -- the question is authored in ordinary spelled-out English instead, so it shares no unsplit token with the item's own text and can only gain overlap from camelCase splitting being enabled. LIMITATION (agent-estate#1154 fix pass): this case's authoring honesty is not automatically measurable -- there is no target_text, so goldenquery computes no overlap line for it. Manual read-only review of this case's own target body against its question measured roughly 53% content-word overlap, substantially derivative of the target beyond the camelCase term the case is meant to test. That derivativeness makes this an easy question likely to rank near the top even under a much weaker scorer, which weakens this case's power to catch a real regression, not just its authoring honesty -- a HIT here is not strong evidence against a regression.",
    "rationale": "corpus:item:it-1b08d9b19b25eee7 is a hard, live (weight=hard, status=acted) directive about exactly this timeout-vs-silent-failure question for dispatch delivery to an agent; grep confirms the identifying camelCase term appears in no other live item across any of the five compiled sources, so it is the one unambiguous answer regardless of whether the current ranking finds it."
  },
  {
    "id": "camelcase-02",
    "question": "What is supposed to happen if the step that hands a chat message off to its agent fails after the message's own thread row has already been written?",
    "expected_source": "corpus-directive",
    "expected_identifier": "corpus:item:it-02f3650145ef2d30",
    "note": "Same camelCase-collision stratum as camelcase-01 -- the item's own body names the chat-dispatch step with a single run-together identifier; this question uses ordinary spelled-out English instead and quotes none of the item's own text. LIMITATION (agent-estate#1154 fix pass): not automatically measurable -- no target_text, so goldenquery computes no overlap line. Manual read-only review measured roughly 57% content-word overlap between this case's question and its target body, substantially derivative beyond the camelCase term under test, which weakens this case's power to catch a real regression, not just its authoring honesty -- a HIT here is not strong evidence against a regression.",
    "rationale": "corpus:item:it-02f3650145ef2d30 is a hard, live (weight=hard, status=acted) sweep directive naming this exact failure path -- a chat-dispatch rejection arriving after the thread row is already persisted. Grep confirms its identifying camelCase term appears in no other live item, so it is the one unambiguous answer regardless of whether the current ranking finds it."
  },
  {
    "id": "camelcase-03",
    "question": "Was a particular agent-facing Claude config component cleared as a non-issue because its environment variables and agent id are both read fresh at call time rather than captured once and reused?",
    "expected_source": "corpus-directive",
    "expected_identifier": "corpus:item:it-125bac07eb029777",
    "note": "Same camelCase-collision stratum -- the item's own body names the component and its two fields with three separate run-together identifiers; this question spells all three out as ordinary English instead of quoting any of them. LIMITATION (agent-estate#1154 fix pass): not automatically measurable -- no target_text, so goldenquery computes no overlap line. Manual read-only review measured roughly 30% content-word overlap between this case's question and its target body -- the lowest of the five, but still not automatically checked -- and the question still reuses the target's overall shape beyond the camelCase term under test, which weakens this case's power to catch a real regression, not just its authoring honesty.",
    "rationale": "corpus:item:it-125bac07eb029777 is a hard, live (weight=hard, status=acted) PR note recording exactly this negative result -- a named config component checked and cleared because two of its fields are read at call time, not captured stale. Grep confirms its three identifying camelCase terms appear in no other live item, so it is the one unambiguous answer regardless of whether the current ranking finds it."
  },
  {
    "id": "camelcase-04",
    "question": "Is there a cheap assertion that a render-phase adjustment only fires once per thread id change, instead of on every render, so that page's flakiness stops being chased?",
    "expected_source": "corpus-directive",
    "expected_identifier": "corpus:item:it-a9e4691c8306e609",
    "note": "Same camelCase-collision stratum -- the item's own body names the changing value with a single run-together identifier joining two ordinary English words; this question spells it out as 'thread id' instead of quoting the item's own token. LIMITATION (agent-estate#1154 fix pass): not automatically measurable -- no target_text, so goldenquery computes no overlap line. Manual read-only review measured roughly 67% content-word overlap between this case's question and its target body -- at the github-stars failure-mode threshold agent-estate#1115 identified -- with several 3-4 word verbatim runs shared beyond the camelCase term under test, which weakens this case's power to catch a real regression, not just its authoring honesty -- a HIT here is not strong evidence against a regression.",
    "rationale": "corpus:item:it-a9e4691c8306e609 is a hard, live (weight=hard, status=acted) directive proposing exactly this cheap assertion for a flaky render path. Grep confirms its identifying camelCase term appears in no other live item, so it is the one unambiguous answer regardless of whether the current ranking finds it."
  },
  {
    "id": "camelcase-05",
    "question": "For that model-creation bug, does the POST path decide whether a model counts as a platform model at creation time, while the PUT path has to read the existing row's value instead of re-deriving it?",
    "expected_source": "corpus-directive",
    "expected_identifier": "corpus:item:it-a0e34b1721ee4bb6",
    "note": "Same camelCase-collision stratum -- the item's own body names the derived flag with a single run-together identifier joining three ordinary English words; this question spells it out as 'platform model' instead of quoting the item's own token. LIMITATION (agent-estate#1154 fix pass): not automatically measurable -- no target_text, so goldenquery computes no overlap line. Manual read-only review measured roughly 65% content-word overlap between this case's question and its target body, close to the github-stars failure-mode threshold agent-estate#1115 identified and substantially derivative beyond the camelCase term under test, which weakens this case's power to catch a real regression, not just its authoring honesty -- a HIT here is not strong evidence against a regression.",
    "rationale": "corpus:item:it-a0e34b1721ee4bb6 is a hard, live (weight=hard, status=acted) directive fixing exactly this POST-vs-PUT asymmetry for a derived flag. Grep confirms its identifying camelCase term appears in no other live item, so it is the one unambiguous answer regardless of whether the current ranking finds it."
  },
  {
    "id": "none-01",
    "question": "Wingspan in centimeters of an adult resplendent quetzal specimen?",
    "expected_source": "none",
    "expected_identifier": "",
    "note": "agent-estate#1333: REPLACES the original question ('What's the office vending machine restocking schedule?'), which stopped being a genuine no_match as the vault grew -- 'machine' alone now coincidentally shares a title term with several unrelated facts (agent-estate#1134's title-floor exemption lets a single shared TITLE term through minMatchedTerms even for an otherwise-sparse source). Confirmed on 2026-09-10 that the old question no longer round-trips: --private mode returns a WEAK MATCH (exit 0, not no_match), default/public mode returns StateWithheldPrivate (exit 3, not exit 1) -- both are the query command behaving exactly as designed (agent-estate#1037/#1306), not a bug; the fixture's premise had simply gone stale. Four candidate replacements using ordinary vocabulary (kombucha/SCOBY fermentation, a vending-machine variant, a bird-fact variant using common connecting words like 'average'/'kept'/'describe') were tried and rejected -- each still coincidentally matched a vault-fact title on at least one connecting word, confirming this is a real, reproducible effect of vault scale (3,203 vault facts as of 2026-09-10, each with its own natural-language title), not a fluke of the original question. This replacement was built by first extracting every distinct word appearing in any vault-fact/repo-docs/loops-research TITLE field in the compiled index (5,584 distinct words) and choosing question vocabulary entirely outside that set. Verified absent from all five sources on 2026-09-10: zero vault-fact or Loops-Research body matches for 'wingspan'/'centimeter'/'resplendent'/'quetzal'/'specimen' (grep -ril), zero live_parameters rows (sqlite3 COUNT(*)=0), zero github-stars descriptions in the compiled index, zero AGENTS.md/docs/*.md matches (grep -ril) -- AND, unlike the retired question, actually exits 1/no_match in both --private and default/public mode against the live index (not merely absent from a grep).",
    "rationale": "Chosen for zero plausible overlap with this estate's own operational vocabulary (software delivery, agent dispatch, tmux, PRs, the vault/corpus system itself) -- ornithological measurement trivia shares no domain with anything this index exists to answer. This is the required no_match case -- the honest answer is that nothing in the index answers it."
  }
]
