{"date":"2026-08-22","source":"walk agent-b5.md re-run (main)","verdict":"RENDERS","notes":"Unchanged -- real STATE/MODE data for live lanes (e.g. estate:build-2..5 busy, Hill90:supervisor), MODE column showing local for real running lanes."}
{"date":"2026-08-23","source":"navcheck.md full-walk re-drive (main)","verdict":"RENDERS","notes":"Unchanged -- real live lane rows (estate:director, build-2..5, tui-demo:bash), real STATE/MODE columns."}
{"date":"2026-08-23","source":"estate-loop/b4-tui-postmerge-vhs.md full-walk re-drive, post #140/#141 (main bffb08c) (2026-08-23)","verdict":"RENDERS","notes":"Unchanged, and specifically re-checked post-#140/#141: STATE column stays live at the fast 2s cadence after #140 split cost onto its own slow ticker (confirmed: estate:build-5 changed free-\u003ebusy between a 2s and a 10s capture). MODE column reads 'local' for every row via #141's new execution_mode decode/fallback path -- but the field itself is not yet emitted anywhere: agent-supervisor's live checkout has no execution_mode in lanes.sh (agent-supervisor#534, the daemon-side emitter, is still blocked by the authorship gate and unmerged), so every row is resolving through the OLD Command/State fallback, not genuinely exercising #141's new field-based path. COULD NOT MEASURE the 'container' arm (no AgentBox driver exists anywhere in this estate) or a genuine field-sourced 'local'/'unknown' distinction (the field is dormant) -- both arms are synthetic/unverifiable against live data until #534 merges; #141's own unit tests already mutation-check the decode logic itself, not re-derived here. COST column reads 'unknown' for every row through a 25s hold -- confirmed this is honest degradation, not a #140 regression: every estate:* lane's harness_session_id is empty in the live ledger (queried directly), which buildAgentCostFetch's own early-return ('no lane has a resolved harness session id yet') already treats as a real zero-cost-rows state, independent of the fetch cadence."}
{"date":"2026-08-23","source":"estate-loop/b4-tui-postmerge-vhs.md full-walk re-drive, post #140/#141 (main bffb08c)","verdict":"RENDERS","notes":"Unchanged, and specifically re-checked post-#140/#141: STATE column stays live at the fast 2s cadence after #140 split cost onto its own slow ticker (confirmed: estate:build-5 changed free-\u003ebusy between a 2s and a 10s capture). MODE column reads 'local' for every row via #141's new execution_mode decode/fallback path -- but the field itself is not yet emitted anywhere: agent-supervisor's live checkout has no execution_mode in lanes.sh (agent-supervisor#534, the daemon-side emitter, is still blocked by the authorship gate and unmerged), so every row is resolving through the OLD Command/State fallback, not genuinely exercising #141's new field-based path. COULD NOT MEASURE the 'container' arm (no AgentBox driver exists anywhere in this estate) or a genuine field-sourced 'local'/'unknown' distinction (the field is dormant) -- both arms are synthetic/unverifiable against live data until #534 merges; #141's own unit tests already mutation-check the decode logic itself, not re-derived here. COST column reads 'unknown' for every row through a 25s hold -- confirmed this is honest degradation, not a #140 regression: every estate:* lane's harness_session_id is empty in the live ledger (queried directly), which buildAgentCostFetch's own early-return ('no lane has a resolved harness session id yet') already treats as a real zero-cost-rows state, independent of the fetch cadence."}
{"date":"2026-08-23","source":"estate-loop/b4-tui-postmerge-vhs.md full-walk re-drive, post #140/#141 (main bffb08c)","verdict":"RENDERS","notes":"Unchanged, and specifically re-checked post-#140/#141: STATE column stays live at the fast 2s cadence after #140 split cost onto its own slow ticker (confirmed: estate:build-5 changed free-\u003ebusy between a 2s and a 10s capture). MODE column reads 'local' for every row via #141's new execution_mode decode/fallback path -- but the field itself is not yet emitted anywhere: agent-supervisor's live checkout has no execution_mode in lanes.sh (agent-supervisor#534, the daemon-side emitter, is still blocked by the authorship gate and unmerged), so every row is resolving through the OLD Command/State fallback, not genuinely exercising #141's new field-based path. COULD NOT MEASURE the 'container' arm (no AgentBox driver exists anywhere in this estate) or a genuine field-sourced 'local'/'unknown' distinction (the field is dormant) -- both arms are synthetic/unverifiable against live data until agent-supervisor#534 merges; #141's own unit tests already mutation-check the decode logic itself, not re-derived here. COST column reads 'unknown' for every row through a 25s hold -- confirmed this is honest degradation, not a #140 regression: every estate:* lane's harness_session_id is empty in the live ledger (queried directly), which buildAgentCostFetch's own early-return ('no lane has a resolved harness session id yet') already treats as a real zero-cost-rows state, independent of the fetch cadence. Correction from the prior recorded observation: fixed a bare '#534' citation (agent-tui's own cross-repo reference guard, TestBareIssueReferencesResolveInThisRepository, correctly caught it) -- qualified as agent-supervisor#534 throughout. A separate, real finding surfaced by a reviewer re-driving this same tape further than this observation did: agent-tui#145, internal/rail's own cost widget (a different package, a different ccusage subcommand) fires 3x in under 30s despite its own 5-minute ticker -- independently reproduced, filed separately, out of scope for this docs-only re-verification."}
{"date":"2026-08-23","source":"estate-loop/b4-tui-postmerge-vhs.md full-walk re-drive, post #140/#141 (main bffb08c)","verdict":"RENDERS","notes":"Unchanged, and specifically re-checked post-#140/#141: STATE column stays live at the fast 2s cadence after #140 split cost onto its own slow ticker (confirmed: estate:build-5 changed free-\u003ebusy between a 2s and a 10s capture). MODE column reads 'local' for every row via #141's new execution_mode decode/fallback path -- but the field itself is not yet emitted anywhere: agent-supervisor's live checkout has no execution_mode in lanes.sh (agent-supervisor#534, the daemon-side emitter, is still blocked by the authorship gate and unmerged), so every row is resolving through the OLD Command/State fallback, not genuinely exercising #141's new field-based path. COULD NOT MEASURE the 'container' arm (no AgentBox driver exists anywhere in this estate) or a genuine field-sourced 'local'/'unknown' distinction (the field is dormant) -- both arms are synthetic/unverifiable against live data until agent-supervisor#534 merges; #141's own unit tests already mutation-check the decode logic itself, not re-derived here. COST column reads 'unknown' for every row through a 25s hold -- confirmed this is honest degradation, not a #140 regression: every estate:* lane's harness_session_id is empty in the live ledger (queried directly), which buildAgentCostFetch's own early-return ('no lane has a resolved harness session id yet') already treats as a real zero-cost-rows state, independent of the fetch cadence. Correction from the prior recorded observation: the earlier note cited the daemon-side emitter with an unqualified issue number in one spot (agent-tui's own cross-repo reference guard, TestBareIssueReferencesResolveInThisRepository, correctly caught it as unresolved in this repository) -- every mention above is now qualified as agent-supervisor#534. A separate, real finding surfaced by a reviewer re-driving this same tape further than this observation did: agent-tui#145, internal/rail's own cost widget (a different package, a different ccusage subcommand) fires 3x in under 30s despite its own 5-minute ticker -- independently reproduced, filed separately, out of scope for this docs-only re-verification."}
{"date": "2026-08-25", "source": "estate-loop/b4-tui-postmerge-vhs.md full-walk re-drive, post agent-tui#140/agent-tui#141 (main bffb08c)", "verdict": "RENDERS", "notes": "Unchanged, and specifically re-checked post-agent-tui#140/agent-tui#141: STATE column stays live at the fast 2s cadence after agent-tui#140 split cost onto its own slow ticker (confirmed: estate:build-5 changed free->busy between a 2s and a 10s capture). MODE column reads 'local' for every row via agent-tui#141's new execution_mode decode/fallback path -- but the field itself is not yet emitted anywhere: agent-supervisor's live checkout has no execution_mode in lanes.sh (agent-supervisor#534, the daemon-side emitter, is still blocked by the authorship gate and unmerged), so every row is resolving through the OLD Command/State fallback, not genuinely exercising agent-tui#141's new field-based path. COULD NOT MEASURE the 'container' arm (no AgentBox driver exists anywhere in this estate) or a genuine field-sourced 'local'/'unknown' distinction (the field is dormant) -- both arms are synthetic/unverifiable against live data until agent-supervisor#534 merges; agent-tui#141's own unit tests already mutation-check the decode logic itself, not re-derived here. COST column reads 'unknown' for every row through a 25s hold -- confirmed this is honest degradation, not a agent-tui#140 regression: every estate:* lane's harness_session_id is empty in the live ledger (queried directly), which buildAgentCostFetch's own early-return ('no lane has a resolved harness session id yet') already treats as a real zero-cost-rows state, independent of the fetch cadence. Correction from the prior recorded observation: the earlier note cited the daemon-side emitter with an unqualified issue number in one spot (agent-tui's own cross-repo reference guard, TestBareIssueReferencesResolveInThisRepository, correctly caught it as unresolved in this repository) -- every mention above is now qualified as agent-supervisor#534. A separate, real finding surfaced by a reviewer re-driving this same tape further than this observation did: agent-tui#145, internal/rail's own cost widget (a different package, a different ccusage subcommand) fires 3x in under 30s despite its own 5-minute ticker -- independently reproduced, filed separately, out of scope for this docs-only re-verification. Citation-format correction (agent-tui#160 fix-pass, fixpass-160.md): this route's own Source field and several Notes citations carried bare #140/#141 that agent-tui#158's qualification pass never reached (agent-tui#158 hand-edited testdata/vhs/full-nav-walk-report.md directly instead of fixing the observation this row is generated from -- discovered when this fix-pass's own required `go run ./cmd/navwalk` regeneration silently reverted agent-tui#158's fix here). No change to what was actually observed, only to how the citations are written -- restoring the exact qualified text agent-tui#158 already reviewed and merged."}
{"date":"2026-08-29","source":"PR agent-tui#179 (at179-navwalk full-nav-walk re-drive, main a5254c0, post agent-tui#176/#178)","verdict":"RENDERS","notes":"CHANGED from baseline: this drive hit '! sessions unavailable: mcp: tools/call: no reply within 10s' and rendered agent-tui#176's new stale-label '(showing last good data, age: 22s)' -- exactly the behavior agent-tui#176 added, confirmed live, not merely unit-tested. MODE still reads 'local' for every row, but the premise behind the old note ('execution_mode not emitted by live lanes.sh') no longer holds: agent-estate#534 (execution_mode field, container-signal) is now MERGED and bootstrap-session.sh writes @hill90_lane_execution_mode=local on every real lane, so 'local' is now plausibly the genuine field-sourced value, not the old Command/State fallback (internal/agents/row.go's modeFor reads the field first) -- could not fully distinguish field-sourced from fallback from the UI alone, but the old blocker is gone. COST is NOT 'unknown' for every row this time (contradicts the old note) -- many rows show real dollar figures ($0.59, $0.72, $1.38, $2.97, $5.28 etc), though this is cached/last-good data per the stale banner, not a freshly confirmed live fetch. Root cause of the mcp timeout: agent-tui#177 (open) -- rail and dashboard remain unguarded callers of the single-threaded mcp_server.py; agent-tui#176 only fixed the Agents pane's own self-inflicted overlap."}
{"date":"2026-08-29","source":"PR agent-tui#182 (at182-stale182, full-nav-walk re-drive, main d745eb6, post agent-tui#180)","verdict":"STALE","notes":"Reproduced live, twice in one session: an isolated single-tape drive (testdata/vhs/agents.tape) came back clean both on load and after [r] refresh -- agent-tui#180's client-side tools/call serialisation (merged since agent-tui#179/#181's original observation) genuinely reduces the contention. But driving the FULL nav walk (testdata/vhs/full-nav-walk.tape, all 24 destinations in one continuous session) reproduced the exact agent-tui#176 stale-labelled state again: '! sessions unavailable: mcp: tools/call: no reply within 10s' plus '(showing last good data, age: 24s)', with the full 22-row table still rendering underneath the banner -- unchanged from the prior RENDERS-classified observation, now correctly STALE instead. This confirms the state is reachable by design (heavier cross-pane MCP load, not a fluke) exactly as agent-tui#182 predicted, not something agent-tui#180 eliminated."}
{"date": "2026-08-29", "source": "PR agent-tui#184 (agent-estate:2, full-nav-walk re-drive, main 32e0f12, post agent-tui#183)", "verdict": "RENDERS", "notes": "Re-driven per agent-tui#184's brief: the prior STALE observation on this route was stamped against d745eb6 (post-agent-tui#180, pre-agent-tui#183), where the full 24-destination walk reproduced agent-tui#176's stale-labelled state ('! sessions unavailable ... (showing last good data, age: 24s)'). agent-tui#183 self-throttles exactly the two tickers (monitor, chat) whose background sessions polling supplied the cross-pane contention that reproduced it. Re-drove testdata/vhs/full-nav-walk.tape (the same 24-destination tape, one continuous session, no env override) against merged main at 32e0f12: 02-agents.png shows a clean populated 24-row table, no error banner, no '(showing last good data, age: N)'. STALE does not reproduce on this route today -- read as agent-tui#183 working as designed, not the original d745eb6 observation having been wrong. The STALE term itself stays in the vocabulary (agent-tui#176 built it deliberately, reachable by design, not contingent on one drive reproducing it) even though no route currently carries it -- see the Lanes observation and the report's own history note for the same reasoning."}
